{
  "name": "bitcoin-example",
  "version": "0.1.0",
  "private": true,
  "dependencies": {
    "normalize.css": "^7.0.0",
    "pusher-js": "^4.2.2",
    "react": "^16.2.0",
    "react-dom": "^16.2.0",
    "react-flexbox-grid": "^2.0.0",
    "react-scripts": "1.1.0",
    "string_decoder": "^1.0.3",
    "styled-components": "^2.4.0"
  },
  "scripts": {
    "start": "react-scripts start",
    "build": "react-scripts build",
    "test": "react-scripts test --env=jsdom",
    "eject": "react-scripts eject",
    "deploy":
      "now alias $(now build --public --name bitcoin-example --dotenv --token $NOW_TOKEN) bitcoin-apmyp --token $NOW_TOKEN"
  },
  "devDependencies": {
    "eslint-config-react-app": "^2.1.0"
  }
}
